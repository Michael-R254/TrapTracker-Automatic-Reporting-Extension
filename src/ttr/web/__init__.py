"""Minimal web demo for the report generator (viva artifact only — not a product,
no auth, no deployment). It calls the SAME RetrievalAgent / ReportGeneratorAgent
the CLI does (the §5.4 reusability test); it holds NO report logic of its own.
"""
